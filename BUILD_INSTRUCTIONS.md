# 🚀 KNOUX VERSA - Build Instructions

## 📋 نظرة عامة

KNOUX VERSA هو محرر صور بالذكاء الاصطناعي يعمل محلياً بدون إنترنت، مع 30 أداة ذكية متقدمة، تصميم فخم بتقنية Glassmorphism، ودعم كامل للعربية والإنجليزية.

## 🛠️ متطلبات النظام

### للتطوير:

- Node.js 18.0.0 أو أحدث
- npm 9.0.0 أو أحدث
- Windows 10/11 (للتغليف كـ EXE)
- Git

### للتشغيل النهائي:

- Windows 10/11 (64-bit)
- 4GB RAM (8GB مستحسن)
- 2GB مساحة خالية
- معالج رسوميات يدعم DirectX 11

## 📦 تثبيت التبعيات

```bash
# استنساخ المشروع
git clone [repository-url]
cd knoux-versa

# تثبيت التبعيات
npm install

# إعداد قاعدة البيانات (اختياري)
npm run db:push
```

## 🔧 سكريپتات البناء

### 1. تطوير محلي

```bash
# تشغيل الخادم للتطوير
npm run dev

# تشغيل Electron في وضع التطوير
npm run electron:dev
```

### 2. بناء الإنتاج

```bash
# بناء الواجهة الأمامية
npm run build

# بناء وتغليف Electron
npm run electron:build

# تغليف كامل لـ Windows EXE
npm run build-exe
```

### 3. سكريپتات إضافية

```bash
# تنظيف مجلدات البناء
npm run clean-build

# تغليف Windows فقط
npm run package-win

# فحص الأنواع
npm run check
```

## 🏗️ عملية التغليف الكاملة

### الخطوة 1: التحضير

```bash
# تأكد من وجود جميع الملفات
npm run check

# نظف البناءات السابقة
npm run clean-build
```

### الخطوة 2: التغليف

```bash
# تغليف كامل إلى EXE
npm run build-exe
```

هذا السكريپت سيقوم بـ:

1. ✅ فحص التبعيات وتثبيت الناقص
2. 🧹 تنظيف مجلدات البناء السابقة
3. ⚛️ بناء الواجهة الأمامية (React + Vite)
4. 🖥️ بناء العملية الرئيسية (Electron)
5. 📁 نسخ الملفات الأساسية (VIP key, الأيقونات)
6. 📦 تغليف التطبيق باستخدام electron-builder
7. ✅ فحص الملف النهائي

### الخطوة 3: النتيجة

```
📁 release/
   ├─ KNOUX VERSA Setup 1.0.0.exe     # مثبت كامل
   ├─ KNOUX VERSA 1.0.0.exe           # نسخة محمولة
   └─ win-unpacked/                    # ملفات غير مضغوطة
```

## 📂 هيكل المشروع

```
knoux-versa/
├─ client/                    # الواجهة الأمامية (React)
│  ├─ src/
│  │  ├─ components/         # مكونات الواجهة
│  │  ├─ pages/              # صفحات التطبيق
│  │  ├─ hooks/              # React Hooks
│  │  ├─ lib/                # مكتبات مساعدة
│  │  ├─ data/               # قواعد البيانات المحلية
│  │  └─ shared/             # أنواع مشتركة
│  └─ index.html
├─ server/                   # الخادم الخلفي (Express)
├─ electron/                 # عملية Electron الرئيسية
│  ├─ main.ts               # النافذة الرئيسية
│  └─ preload.js            # سكريپت التحميل المسبق
├─ scripts/                  # سكريپتات البناء
│  ├─ build-electron.js     # بناء Electron
│  └─ make-exe.js           # تغليف EXE
├─ assets/                   # الأصول (أيقونات، صور)
├─ dist/                     # مخرجات البناء
├─ release/                  # التطبيق النهائي
└─ vip.key                   # مفتاح VIP
```

## 🔑 نظام VIP

التطبيق يدعم وضعين:

- **Standard**: جميع الأدوات الأساسية (28 أداة)
- **VIP**: أدوات إضافية حصرية + ميزات متقدمة

### تفعيل VIP:

1. تأكد من وجود `vip.key` في مجلد التطبيق
2. المفتاح الحالي: `SADEK_ELGAZAR_VIP_2025`
3. في التطبيق، اختر أداة VIP وأدخل المفتاح

## 🎨 التخصيص

### الألوان والتصميم:

- ملف `client/src/index.css` - الأنماط الرئيسية
- `tailwind.config.ts` - إعدادات Tailwind CSS
- Glassmorphism تلقائي مع تأثيرات النيون

### اللغات:

- `client/src/components/LanguageProvider.tsx` - إضافة ترجمات
- دعم RTL للعربية تلقائي

### الأدوات:

- `client/src/data/aiToolsDatabase.ts` - إضافة أدوات جديدة
- `client/src/lib/toolProcessors.ts` - ��عالجات الأدوات

## 🐛 استكشاف الأخطاء

### مشاكل شائعة:

**1. فشل البناء:**

```bash
# نظف وأعد تثبيت التبعيات
npm run clean-build
rm -rf node_modules package-lock.json
npm install
npm run build-exe
```

**2. مشكلة Electron:**

```bash
# إعادة بناء التبعيات الأصلية
npm run electron:rebuild
```

**3. مشكلة التغليف:**

```bash
# تأكد من وجود أدوات Windows Build
npm install --global windows-build-tools
```

**4. مشكلة الأيقونة:**

- تأكد من وجود `assets/icon.ico`
- حجم الأيقونة يجب أن يكون 256x256 أو أصغر

### سجلات التصحيح:

```bash
# تشغيل مع سجلات مفصلة
DEBUG=electron-builder npm run build-exe

# فتح أدوات التطوير في Electron
npm run electron:dev
```

## 🚀 التوزيع

### للمستخدمين النهائيين:

1. احفظ الـ `.exe` النهائي
2. تأكد من تضمين `vip.key` (للميزات VIP)
3. التطبيق يعمل بدون تثبيت (محمول)
4. لا يحتاج إنترنت للتشغيل

### للمطورين:

1. يمكن توزيع الكود المصدري
2. جميع التبعيات مفتوحة المصدر
3. يمكن تخصيص الأدوات والواجهة

## 📄 الترخيص

MIT License - مفتوح المصدر مع حرية التعديل والتوزيع.

## 👨‍💻 المطور

**Sadek Elgazar (KNOUX)**

- AI Engineer & Modern Software Developer
- Abu Dhabi, UAE 🇦🇪

---

**💡 نصيحة:** لأفضل أداء، شغل التطبيق على جهاز مع معالج رسوميات منفصل (GTX 1060 أو أفضل) لتسريع معالجة الذكاء الاصطناعي.

**🔥 كلمة السر للنجاح: "حرية بلا حدود مع KnouxAI" 🔥**
